<%= form_for(@case) do |f| %>
  <% if @case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case.errors.count, "error") %> prohibited this case from being saved:</h2>

      <ul>
      <% @case.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :docketno %><br />
    <%= f.text_field :docketno %>
  </div>
  <div class="field">
    <%= f.label :casetype %><br />
    <%= f.text_field :casetype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
